# python-keylloger

O script foi construído no Windows 7 x64 usando Python 3.7

Para gerar o executável do script python execute o seguinte comando:

`pyinstaller -w -F -i arquivo.ico nome_script.py`
